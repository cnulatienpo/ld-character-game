services:
  - type: web
    name: character-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd character_api && uvicorn server.main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.11
