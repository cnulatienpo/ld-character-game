/// obj_reading_continue : Left Pressed Event
// Placeholder action that should link back to the lesson or next activity.
if (point_in_rectangle(mouse_x, mouse_y, x, y, x + button_width, y + button_height)) {
    show_debug_message("Reading room continue pressed.");
}
